# 合約＋報價單自動化系統：操作守則

（2026-08-07 版）

一句話理解這套系統：**在 Notion 把資料填好 → 按一下按鈕 → 15~30 秒後 PDF 出現在同一列。**
合約和報價單是**兩套完全獨立的系統**（不同資料庫、不同流程），但操作方式一樣。

---

## 一、產生合約（資料庫：合約測試）

### 填資料

**客戶基本資料**：中文姓名（標題欄）、護照英文姓名、西元生日、護照號碼、通訊地址、電話、Email、陪客-大／陪客-小（人數）、緊急聯絡人＋電話、過敏原（有填會以紅字寫進第六條告知紀錄；沒填則印手寫空格線）

**預算表**：
- 待產入住日期／月子入住日期 → **選日期區間（起～迄）**，合約上會印「X 年 X 月 X 日 至 X 年 X 月 X 日」；只填單日也可以
- 待產天數／月子天數 → 公式自動算，**不用填**（注意：目前公式是頭尾相減，不含最後一天）
- 待產房型／月子房型、每日房費、定金 → 手動填
- 月子中心 → 選 **Charlie 或 Yoyo**（⚠️ Johnny 合約模板尚未製作，暫時不要選 Johnny 產合約）

**證件照** → 直接上傳圖片到此欄位（會自動出現在合約的證件影本頁，壓客戶姓名紅色浮水印）

### 產生

按「🖨 產生合約」按鈕（等同把「產生合約」打勾）→ 約 15~30 秒 → 「合約 PDF」欄出現檔案、「合約狀態」變「已產生」、勾自動取消。

- 合約金額全自動算：小計、總房費、頭款、期款（Charlie 90/10、Yoyo 70/30，依月子中心自動套用對應模板與算式）
- 每頁壓「（客戶姓名）專用」灰色浮水印

### 重新生成

改完資料**再按一次按鈕**即可，新 PDF 會覆蓋舊檔。10 秒內連按多次只會生成一次（防連點）。

### 合約上保留手寫的欄位（不用找哪裡填）

第幾胎、自然產/剖腹產、自費醫師、自費醫院、延後入住的「幾時前」（Charlie 版）、簽署區（簽名、身分證字號、戶籍地址、日期）

---

## 二、產生報價單（資料庫：🧾 報價單）

### 填資料

- 客戶姓名（標題欄）
- 待產期間／月子期間 → 選日期區間，天數公式自動算
- **關聯欄位用選的**：月子中心、待產房型、月子房型、醫生、醫院、**費用設定（務必勾「標準」）**
- 生產方式（自然產／剖腹產，影響醫生費／醫院費的公式）
- 陪客大人／小孩的人數與天數、其他費用（有才填）
- 所有金額（房費、醫療、小費、陪客費、總計美元／台幣）都由公式與費率設定自動算，**不用手填**

### 產生前先看「⚠️檢查」欄

- 顯示 **✅** → 可以按
- 顯示 **🚨** → 設定缺東西（最常見：費用設定沒勾標準），此時按了**不會生成**，Notion 上也不會有任何提示，修好再按

### 產生

按「產生報價單」按鈕（＝打勾）→ 約 15~30 秒 → 「報價單 PDF」出現、「報價狀態」變「已產生」。

報價單特有的雙重保險：
1. ⚠️檢查有 🚨 → 拒絕生成
2. **驗算**：系統會用另一套算式重算總計，跟 Notion 公式對不上就拒絕生成（防止公式被改壞後寄出錯誤報價）

### 重新生成

同合約：改資料再按一次，覆蓋舊檔。

---

## 三、常見問題排查

| 狀況 | 原因與處理 |
|---|---|
| 按了沒反應（30 秒後仍沒 PDF） | 先看勾選有沒有被自動收掉：勾還在→系統可能停擺，找管理者看 n8n；勾消失但沒 PDF→大概率被防呆擋下（報價單看 ⚠️檢查），修好再按 |
| 證件照沒出現在合約裡 | 確認「證件照」欄位有檔案，再按一次按鈕重新生成 |
| PDF 內容是舊的 | 那是上次生成的檔案，按一次按鈕重新生成 |
| 改了欄位「名稱」後壞掉 | ⚠️ 欄位名稱跟程式綁定，**任何欄位都不可自行改名／刪除**，要改名請找管理者同步修改 |
| 想連按幾次多出幾份 | 不會多，10 秒防連點只會生成一份 |

---

## 四、安全注意事項

1. **測試期間（現在）只能用假資料**：PDF 轉換目前借用公用測試伺服器，合約內容會經過第三方。正式啟用前管理者會換成自架轉換器（見第五節），換完才可以放真實客戶個資。
2. 產出的 PDF 連結、合約連結都帶**驗證碼**，被竄改參數會顯示錯誤頁，不會產出假合約。
3. 每份文件都壓「客戶姓名專用」浮水印（合約灰色滿版＋證件照紅色），外流可追來源。
4. Notion 的 integration token＝資料庫鑰匙，不要外傳；懷疑外洩立即到 notion.so/profile/integrations 重發。

---

## 五、管理者資訊（維護／交接用）

### 架構

```
Notion 按鈕（打勾）
→ 偵察兵 workflow（每 15 秒查勾選、防重複、先收勾）
→ 主力 workflow（webhook 收頁面 id → 讀 Notion 資料 → 組參數網址
   → Gotenberg 開模板網頁轉 PDF → 上傳回 Notion 檔案欄 → 更新狀態）
```

### 元件清單

| 元件 | 位置 |
|---|---|
| 模板（3 個） | GitHub `smallwins33/contract-generator`：contract.html（Charlie）、contract_yoyo.html（Yoyo）、quote.html（報價單），Pages 公開存取 |
| 合約 workflow | n8n「Notion 合約產生器」＋「合約偵察兵（15 秒）」，webhook 路徑 `/webhook/contract-generate` |
| 報價 workflow | n8n「Notion 報價單產生器」＋「報價偵察兵（15 秒）」，webhook 路徑 `/webhook/quote-generate`，**與合約完全獨立、互不影響** |
| workflow 備份 | repo 內 n8n_workflow_*.json 四個檔案，可直接匯入 n8n |
| PDF 轉換 | ⚠️ 目前 demo.gotenberg.dev（僅測試）。正式：在 n8n 所在的 Zeabur 專案加開 gotenberg/gotenberg 容器，把兩條主力的「轉成 PDF」節點網址換成內部位址 |

### 鐵則

1. **欄位名稱＝程式介面**：Notion 欄位改名、n8n 程式要同步改，兩邊一起動
2. **改 workflow 後要重建不要重啟**：n8n 停用再啟用可能殘留舊計時器造成重複觸發，偵察兵有問題直接刪掉重匯入
3. 合約與報價單**分開維護**，改一邊不會影響另一邊
4. 交接到新環境的流程：對方建 Notion integration → 資料庫開連接 → n8n 匯入 JSON → 重建憑證 → 改資料庫 ID 與 webhook 網址（詳見 n8n_setup_guide.txt）

### 待辦（未完成項目）

- [ ] 正式上線前：換自架 Gotenberg（鐵則等級）
- [ ] Johnny 版合約模板（原始 PDF 已在手上）
- [ ] 天數公式是否要含頭尾（+1）：待業務確認
- [ ] 報價單抬頭品牌名（現為 Melody's US & CA Life，待確認）
- [ ] 防呆擋下時 Notion 端無提示：可加「產生失敗」狀態回寫（未做）
